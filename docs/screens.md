# Screens

画面ごとの仕様。要件は [requirements.md](requirements.md) を正とし、ここでは「何を表示し、何ができ、どの状態をどう見せるか」を決める。

## 共通ルール

### 部品の置き場所

| 層 | 場所 | 中身 |
| -- | -- | -- |
| 汎用の部品 | `src/shared/presentation/components/ui/` | shadcn/ui（Base UI）が生成する部品。掲示板のことを知らない |
| 機能の部品 | `src/modules/<feature>/presentation/` | 掲示板を知っている部品と Server Action |
| 画面 | `src/app/**/layout.tsx`、`page.tsx` | `@/di/*` でデータを取り、機能の部品を並べる |

### Server Component と Client Component

- 既定は Server Component。データは `layout.tsx` / `page.tsx` で取り、props で渡す。
- `"use client"` は、クリック・入力中の状態・ブラウザの機能（Cookie の書き込み、`usePathname`）が必要な末端の部品にだけ付ける。

### 状態の見せ方

| 状態 | 見せ方 |
| -- | -- |
| 未ログイン | `/sign-in` へリダイレクトする（`proxy.ts` とページの両方で確認） |
| 存在しない・所属していない・ID の形式が不正 | 404 画面（区別しない） |
| 入力エラー（`ValidationError`） | フォームの該当項目の近くにメッセージを表示し、入力値は残す |
| 送信中 | 送信ボタンを無効にし、文言を「〜中…」にする |
| 一覧が空 | 空であることと、次にできる操作を1行で示す |

### 配置の考え方

1. よく使う操作ほど、近くて目立つ場所に置く（掲示物の登録・撤去タスクは掲示板のヘッダー）。
2. 目的ごとにまとめる。サイドバーは「どの掲示板を見るか」、ヘッダーは「その掲示板で何をするか」、アカウントメニューは「自分に関すること」。
3. 取り消せない操作ほど遠くに置き、確認を挟む（掲示板の削除は設定画面の奥で確認ダイアログ）。やり直せる操作（ログアウト）に確認は付けない。
4. 独自の配置にする理由がなければ、利用者が他のサービスで見慣れた形に合わせる。
5. スマホで押せる部分は 44px 以上にする。

### 画面幅

- `md`（768px）以上：サイドバーを常に表示する。
- `md` 未満：サイドバーを折りたたみ、上部バーのメニューボタンで開く。

## 画面一覧（ステップ1）

| ID | 画面 | URL | 種類 |
| -- | -- | -- | -- |
| S0 | ログイン | `/sign-in` | 実装済み |
| — | 振り分け | `/boards` | 画面なし（リダイレクトのみ） |
| — | メンバー向けレイアウト（サイドバー） | `/boards` 配下すべて | レイアウト |
| S5 | 掲示板作成 | `/boards/new` | 画面 |
| S2 | 掲示板ボード（メンバー向け、枠のみ） | `/boards/{boardId}` | 画面 |
| — | 404 | 該当なし | 画面 |

---

## 振り分け `/boards`

ログイン後の入口。画面は表示せず、移動先を決めてリダイレクトする（要件 18）。

| 項目 | 内容 |
| -- | -- |
| データ | Cookie `last-board-id` を読み、`findLandingBoard({ userId, lastBoardId })` を呼ぶ |
| 掲示板が返った | `/boards/{boardId}` へリダイレクト |
| `null` が返った | `/boards/new` へリダイレクト |
| ファイル | `src/app/boards/page.tsx`（Server Component） |

---

## メンバー向けレイアウト（サイドバー）

`/boards` 配下のすべての画面に共通の枠。

```
md 以上                                  md 未満
┌──────────────┬───────────────────┐    ┌─────────────────────────┐
│ tackup       │                   │    │ ☰  tackup               │
│              │                   │    ├─────────────────────────┤
│ 掲示板       │                   │    │                         │
│ ▸ 中野のボード│   （各画面の本文）  │    │    （各画面の本文）       │
│   🔒 職員室   │                   │    │                         │
│              │                   │    │                         │
│ ＋ 掲示板を作成│                   │    │                         │
│              │                   │    │                         │
│ ────────     │                   │    │                         │
│ [山] 山田 太郎 ⌃│                   │    │                         │
└──────────────┴───────────────────┘    └─────────────────────────┘
```

### 表示する情報

| 領域 | 内容 | データ |
| -- | -- | -- |
| 上部 | アプリ名 `tackup` | 固定 |
| 掲示板一覧 | 所属する掲示板を、所属した日時の新しい順に並べる | `listMyBoards(userId)` |
| 各項目 | 掲示板名。非公開の掲示板には鍵アイコン | `Board.name`、`Board.isPublic` |
| 現在地 | 表示中の掲示板を強調する（`aria-current="page"`） | URL の `boardId` |
| 作成への導線 | 「＋ 掲示板を作成」→ `/boards/new` | 固定 |
| 下部 | アカウントボタン（アイコン、ユーザー名、開閉の矢印）。押すとアカウントメニューを開く | セッションの `user.name`、`user.email`、`user.image` |

### 状態

- 所属する掲示板がない：一覧の位置に「所属する掲示板はありません」と表示する。
- 掲示板名が長い：1行に収まらない分は省略記号で切り、全体は `title` 属性で確認できるようにする。

### アカウントメニュー

```
閉じているとき                       押したとき（ボタンの上に開く）
┌──────────────────────────┐        ┌──────────────────────────┐
│ [山] 山田 太郎          ⌃ │        │ 山田 太郎                 │
└──────────────────────────┘        │ yamada@example.com        │
                                    │ ───────────────────────── │
                                    │ ⎋ ログアウト               │
                                    └──────────────────────────┘
```

| 項目 | 内容 |
| -- | -- |
| アイコン | `user.image` があれば画像。ない、または読み込めなければ名前の頭文字（名前が空ならメールアドレスの頭文字）を丸の中に表示する |
| メニューの先頭 | ユーザー名とメールアドレス（どのアカウントでログインしているかを確かめられる） |
| ログアウト | 押したらすぐログアウトし、`/sign-in` へ移動する。確認ダイアログは出さない（もう一度ログインすれば戻れるため） |
| 画面幅 | スマホでは、開いたサイドバーの下部に同じボタンを置く |
| 部品 | `ui/avatar`、`ui/dropdown-menu` |

### 部品構成

```
src/app/boards/layout.tsx                    [Server] getSession、listMyBoards
└ BoardSidebar                               [Server] modules/board/presentation
   ├ BoardSidebarLink（現在地の強調）          [Client] usePathname
   ├ 「掲示板を作成」リンク                    ui/button（render={<Link />}）
   └ AccountMenu                              [Client] modules/auth/presentation（ログアウトを含む）
```

`AccountMenu` にはセッションから名前・メールアドレス・画像だけを props で渡す。

`md` 未満の折りたたみは shadcn/ui の `sidebar` の仕組みを使う（開閉ボタンは Client Component）。

---

## S5 掲示板作成 `/boards/new`

要件 21（掲示板管理）、22（公開設定）。

```
┌───────────────────────────────────────────┐
│ 掲示板を作成                               │
│ 掲示物を掲示する場所を作ります。             │
│                                           │
│ 掲示板名 *                                 │
│ ┌───────────────────────────────────────┐ │
│ │ 中野のボード                           │ │
│ └───────────────────────────────────────┘ │
│ 50文字以内                                 │
│                                           │
│ 公開設定                                   │
│ ◉ 非公開  メンバーだけが閲覧できます         │
│ ○ 公開    招待リンクと QR コードから        │
│           ログインなしで閲覧できます         │
│                                           │
│                          [ 作成する ]      │
└───────────────────────────────────────────┘
```

### 入力項目

| 項目 | 部品 | ルール | 初期値 |
| -- | -- | -- | -- |
| 掲示板名 | `ui/input` + `ui/label` | 必須。前後の空白を除いて1〜50文字（`parseBoardName`）。`maxLength` も 50 | 空 |
| 公開設定 | `ui/radio-group` | 非公開 / 公開 のどちらか。それぞれに意味の説明を付ける | 非公開 |

### 操作

| 操作 | 処理 |
| -- | -- |
| 「作成する」 | Server Action `createBoardAction` → `createBoard({ name, isPublic, userId })`。`userId` はフォームではなくセッションから取る |
| 成功 | `/boards/{boardId}` へリダイレクト |
| `ValidationError` | 掲示板名の下にメッセージを表示し、入力値は残す |
| その他のエラー | フォームの上に「掲示板を作成できませんでした。時間をおいて再度お試しください」 |
| 送信中 | ボタンを無効にし「作成中…」 |

### 見出しの出し分け

- 所属する掲示板がない（初回）：説明文を「最初の掲示板を作成しましょう。掲示物を掲示する場所を作ります。」にする。
- それ以外：上の図のとおり。

### 部品構成

```
src/app/boards/new/page.tsx                  [Server] 見出しと CreateBoardForm を並べる
└ CreateBoardForm                            [Client] modules/board/presentation
   └ createBoardAction                       [Server Action] modules/board/presentation/actions.ts
```

---

## S2 掲示板ボード（メンバー向け、ステップ1は枠のみ）`/boards/{boardId}`

要件 12.1。掲示物の表示（masonry）はステップ5、ヘッダーの各導線は対応するステップで追加する。

```
┌───────────────────────────────────────────┐
│ 中野のボード   [非公開]                     │
│                                           │
│                                           │
│        まだ掲示物はありません                │
│                                           │
└───────────────────────────────────────────┘
```

### 表示する情報

| 領域 | 内容 | データ |
| -- | -- | -- |
| ヘッダー | 掲示板名（`h1`）と、公開 / 非公開のバッジ（`ui/badge`） | `getBoard({ boardId, userId })` |
| 本文 | ステップ1では「まだ掲示物はありません」 | 固定 |

### ヘッダーの導線（追加する時期）

| 導線 | 表示条件 | 追加するステップ |
| -- | -- | -- |
| 掲示物登録・撤去タスク・台帳 | メンバー | 4・7・8 |
| 招待リンクの表示・コピー | 公開掲示板 | 9 |
| 招待リンクの再発行・掲示板設定・メンバー管理 | admin | 9 |

ステップ1では、まだ遷移先がない導線は表示しない。

### 最後に開いた掲示板の記録

- 画面が表示されたら、Cookie `last-board-id` にこの掲示板の ID を保存する。
- 属性：`path=/`、`max-age` 1年、`SameSite=Lax`。
- ブラウザで書き込む（Server Component では Cookie を書けないため）。サイドバーのリンクの先読みでは記録されない。

### 状態

| 状態 | 見せ方 |
| -- | -- |
| `getBoard` が `NotFoundError` | 404 画面 |

### 部品構成

```
src/app/boards/[boardId]/page.tsx            [Server] getBoard。NotFoundError なら 404
├ BoardHeader                                [Server] modules/board/presentation
└ RememberLastBoard                          [Client] modules/board/presentation（Cookie を書く。何も表示しない）
```

---

## 404

| 項目 | 内容 |
| -- | -- |
| 文言 | 「ページが見つかりません」「URL が間違っているか、閲覧する権限がありません。」 |
| 導線 | ログイン中なら「掲示板へ戻る」→ `/boards`、未ログインなら「トップへ」→ `/` |
| 注意 | 掲示板が存在するのか、所属していないだけなのかは区別して見せない |

---

## 決めたこと（要件に追記していない仕様）

- ログアウトはサイドバー下部のアカウントメニューの中に置き、確認ダイアログは出さない。
- アイコンは Google の画像を使い、画像がない場合（将来メールアドレスとパスワードの認証を加えた場合を含む）は名前の頭文字を表示する。
- 最後に開いた掲示板の Cookie は `last-board-id`（1年）。
- 公開設定はラジオボタンで選び、それぞれの意味を説明する（切り替えスイッチでは「公開」の意味が伝わりにくいため）。
- まだ遷移先のない導線は、無効表示ではなく非表示にする。
